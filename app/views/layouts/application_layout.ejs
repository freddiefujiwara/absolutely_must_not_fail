<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%- stylesheet_link_tag('http://twitter.github.com/bootstrap/1.3.0/bootstrap.min.css') %>
    <%- stylesheet_link_tag('http://ajax.googleapis.com/ajax/libs/jqueryui/1.8/themes/black-tie/jquery-ui.css') %>
    <%- stylesheet_link_tag('youtube-player') %>
    <%- csrf_meta_tag() %>
  </head>
  <body>
    <div class="container">
      <% var flash = request.flash('info').pop(); if (flash) { %>
      <div class="alert-message info">
        <a class="close" href="#">×</a>
        <%- flash %>
      </div>
      <% } %>

      <% flash = request.flash('error').pop(); if (flash) { %>
      <div class="flash error">
        <a class="close" href="#">×</a>
        <%- flash %>
      </div>
      <% }; %>

      <%- body %>

    </div>
  </body>
  <script>
    $('.alert-message a.close').click(function () {
      $(this).parent().fadeOut('slow');
    });
  </script>
</html>
